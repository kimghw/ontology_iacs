## 2026-04-17T04:05Z — md2wu /home/kimghw/ontology_iacs/UR (session 210d725e)

### 모호/해석 보고

- **모호점**: 사용자는 `/md2wu /home/kimghw/ontology_iacs/UR`만 지정. `batch_capacity`, `source_family` 확정 여부는 명시 없음.
  - **해석**: AskUserQuestion으로 두 가지를 동시에 확인 — (1) Stage 3 `iacs_ur` 승인 여부, (2) `batch_capacity` 값.
  - **사용자 선택**: `iacs_ur` 승인 + `batch_capacity = 600K`.
  - **심각도**: 하 (대화로 즉시 해소).

- **SKILL.md vs phase_c_execute.py 불일치 (사용자 지적 → 수정 완료)**
  - **내용**: SKILL.md §"전역 발행 정책"은 `skill_md2wu/` 루트에 `wu-*__pre__content.md` + `merge_index.json` **두 종류만** 남기도록 규정. 최초 1차 실행 스크립트는 scope별 `corpus-*__pre__manifest.json`, `corpus-*__pre__item_index.json`, `corpus-*__md2wu__issue_gate_report.json`을 전역에 그대로 publish하고 `merge_index.json`은 생성하지 않음.
  - **사용자 지적**: "스킬에 머지랑 스필릿 정보만 남기라고 했는데" — 정책 위반 확인.
  - **조치**:
    1. 기존 workroot 산출물 정리 — T2 manifest·T5 issue_gate 2쌍은 세션 `out/`으로 이동, `corpus-*__pre__item_index.json` 2개는 삭제, 두 scope의 item_to_wu를 합쳐 `merge_index.json` 생성(atomic rename).
    2. [phase_c_execute.py](.claude/skills/md2wu/phase_c_execute.py) 수정 — manifest/issue_gate를 세션 `out/`에만 쓰고, 최종 단계에서 `merge_index.json`을 기존 `corpora`와 병합하여 atomic rename으로 갱신. `corpus-*__pre__item_index.json` 생성 블록 제거.
    3. [phase_b_plan.py](.claude/skills/md2wu/phase_b_plan.py) 수정 — B-0 스킵 판정이 `merge_index.json.corpora[scope].item_to_wu`를 SSOT로 사용(기존 `corpus-*__pre__item_index.json` 글롭 제거).
  - **검증**: 스크립트 수정 후 phase_b_plan 재실행 결과 — 71개(ur_s+ur_z) 정확히 스킵, 남은 209개만 새 배치에 포함됨. 해당 락은 이번 세션 처리 범위 아니므로 즉시 해제.
  - **저장**: `feedback_md2wu_publishing.md` 메모리에 정책 등록.
  - **심각도**: 중 → 해소.

### 수행 결과

| 단계 | 결과 |
|:---|:---|
| Phase A | 280 MD 스캔, 946,777 tokens, 가산성 오류 0건, 경고 8건(±1 BPE 경계) |
| Stage 3 | `iacs_ur` 승인, 기존 classification 매칭 |
| Phase B | 배치 2개. B001(ur_s, ur_z, 71 items, 499,809 tokens) claimed, B002(14개 시리즈, 209 items, 446,968 tokens) reserved_for_other_session |
| Phase C | WUs 20개 publish (ur_z: 12, ur_s: 8). split=1, standalone=11, merged=7. 락 전부 해제. |
| Issue gate | undersized 2건 (iacs_ur_merge_e8a9a124 8,529 tokens / iacs_ur_merge_42418193 13,335 tokens, 심각도 LOW). 자동 `processed`. |

### 후속 필요 작업

- 남은 배치 B002 (ur_a/c/d/e/f/g/h/i/k/l/m/n/p/w, 209 items, 446K tokens)는 **별도 세션**에서 재실행 필요 — 이번 세션은 락을 걸지 않았으므로 다음 세션이 스캔 후 바로 claim 가능.
- SKILL.md "전역 발행 정책"과 스크립트 출력 정렬이 필요하면 별도 지시 요망.

---

## 2026-04-17T04:30Z — md2wu /home/kimghw/ontology_iacs/UI (session ab5fa355)

### 모호/해석 보고

- **모호점 1 — 새 Source Family `iacs_ui` 확정**
  - **사용자 지시**: UI 코퍼스는 md2wu에 처음 투입되는 Source Family.
  - **해석**: `shared/document_classification.md`에 이미 `IACS/UI` DocType이 정의되어 있어 `iacs_ui` 키로 신규 등록 후 진행. [heading_profiles.json](.claude/skills/md2wu/heading_profiles.json)에 `iacs_ui` 프로파일 추가 (L1=문서 제목, L2=Interpretation/NOTE). 기존 `iacs_ur`와 level_mapping 동일.
  - **심각도**: 하 (사용자 승인 후 진행).

- **모호점 2 — Cross-numbered 문서 처리 (MPC104 = MPC+LL+HSC)**
  - **사용자 지시**: "1번은 알아서".
  - **해석**: 파일 경로(`UI_MPC_md/`)를 단일 정본으로 간주. MPC only 등록, LL·HSC 중복 생성하지 않음. `heading_profiles.json`의 `document_boundary.note`에 명시.
  - **심각도**: 하.

- **모호점 3 — 취소 문서 분리 처리**
  - **사용자 지시**: "취소된 문서들만 별도로 묶어서 처리".
  - **해석**: series key에 `_del` 접미사를 붙여(`ui_cc_del`, `ui_sc_del` 등) active 문서와 별도 Merged WU로 분리. 파일명의 `del`/`withdrawn`/`deleted` 토큰을 DEL_RE로 감지.
  - **심각도**: 하.

- **모호점 4 — SC15 중복 파일 (발견된 이슈)**
  - **내용**: `UI_SC_md/UI-SC15-Del-Nov-2005.md`와 `UI-SC15-Del-Nov-2005-1.md`가 바이트 단위로 동일. 둘 다 `iacs_ui_sc15_del_en`로 매핑되어 dict collision 발생.
  - **해석**: 처음 본 경로(`...Nov-2005-1.md`)를 채택, 두 번째는 dedupe. `scan_index.json.duplicates[]`에 기록.
  - **조치**: [phase_a_scan_ui.py](.claude/skills/md2wu/phase_a_scan_ui.py)에 `seen_item_ids` 체크 추가.
  - **심각도**: 중 (같은 내용이므로 결과 영향 없음이나, 원본 디렉토리에서 한쪽 삭제 권장).

### 스크립트 확장

- 기존 `phase_a_scan.py` / `phase_c_execute.py`는 `iacs_ur` 전용 하드코드(glob `UR_*_md/*.md`, 단일 letter series, `iacs_ur_merge_` WU 접두어 등)여서 UR 호환 유지를 위해 신규 파일로 분기:
  - [phase_a_scan_ui.py](.claude/skills/md2wu/phase_a_scan_ui.py) — `UI_*_md/*.md` 스캔, 부모 디렉토리로 series 확정, `del`/`withdrawn` 자동 분리, item_id dedup.
  - [phase_c_execute_ui.py](.claude/skills/md2wu/phase_c_execute_ui.py) — `AUTHORITY=IACS`/`DOC_TYPE=UI`/`GRAMMAR_VERSION=iacs_ui_v01`, Merged WU 접두어 `iacs_ui_merge_`, scope는 `iacs_ui_{series_short}[_del]`.

### 수행 결과

| 단계 | 결과 |
|:---|:---|
| Phase A | 593 MD 스캔 → 592 unique items (SC15 dedup 1건), 318,029 tokens, 가산성 오류 0건, 경고 7건(±1 BPE), skipped_unparseable 0건 |
| Stage 3 | `iacs_ui` 신규 승인, `heading_profiles.json` 등록 |
| Phase B | 단일 배치 B001 (20 series, 592 items, 318K tokens) claimed — 예산 600K 내 전량 수용, reserved 배치 없음 |
| Phase C | WU 27개 publish. wu_type=merged 전부 (개별 UI 문서가 모두 16K 미만), 20개 scope. 락 592개 전부 해제. |
| Issue gate | undersized 17건 전원 LOW. `_del` scope는 대부분 < 100 tokens (콘텐츠가 "deleted" 한 줄). active 중 일부(ui_tm 885, ui_ll_del 361 등)도 하한 미달. 강제 병합 금지 규칙대로 자동 `processed`. |

### scope별 WU 분포

```
ui_sc        : 7 WUs (189,395 tokens)   ← 유일하게 32K 경계 근처로 쪼개짐
ui_ll        : 2 WUs ( 34,404 tokens)
ui_mpc       : 1 WU  ( 20,434 tokens)
ui_gc        : 1 WU  ( 21,583 tokens)
ui_modu      : 1 WU  ( 14,162 tokens)
ui_sc_del    : 1 WU  ( 10,354 tokens)
ui_gf        : 1 WU  (  8,926 tokens)
ui_mpc_del   : 1 WU  (  6,343 tokens)
ui_ftp       : 1 WU  (  5,216 tokens)
ui_hsc       : 1 WU  (  2,423 tokens)
ui_ll        : (상위와 동일 scope, 병합 그룹이 상한 32K에서 2개로 분리됨)
그 외 undersized 소규모 scope: ui_cc 1,610, ui_colreg 1,628, ui_tm 885, ui_ll_del 361,
   ui_colreg_del 55, ui_ftp_del 65, ui_gc_del 44, ui_hsc_del 34,
   ui_modu_del 70, ui_passub_del 37  — 각 1 WU
```

### 후속 필요 작업

- SC15 중복 파일 하나 삭제 권장(`UI-SC15-Del-Nov-2005-1.md` 또는 `UI-SC15-Del-Nov-2005.md`).
- `iacs_ui` 프로파일 `stats` 항목은 본 실행 결과(27 WUs / 592 items / 318K tokens)로 추후 갱신 가능.

---

## 2026-04-17T07:06Z — md2wu /home/kimghw/ontology_iacs/PR (session 9267f956)

### 모호/해석 보고

- **신규 Source Family `iacs_pr` 확정 필요**
  - **모호점**: `heading_profiles.json`에 `iacs_pr` 프로파일 부재. 48개 PR MD 파일은 UR·UI와 파일명 규칙이 상이(`PR-<N>[-Rev.<X>]-<MMM>-<YYYY>-CLN`, `pr<N>rev<M>`, `pr_<NN>_pdf<###>`, `Contact-Details-for-PR1A...`, `PR-<N>-Annex-Rev...`, `pr25-del-...` 등).
  - **해석**: `iacs_pr` Source Family를 신규 등록. series는 단일 `pr`(active) + `pr_del`(deleted/withdrawn). 파일명 13종 변형에 대해 item_id tail 규칙 정의.
  - **사용자 승인 획득**: AskUserQuestion → 승인 + `batch_capacity=600K`.
  - **심각도**: 하.

- **파일명 파싱 버그 발견 → 수정 후 재실행 (중요)**
  - **내용**: 초회 [phase_a_scan_pr.py](.claude/skills/md2wu/phase_a_scan_pr.py)의 `parse_pr_filename` 정규식 `^(\d+[a-z]?)` 이 `pr6rev4.md` 같은 스템에서 `6r`을 숫자+letter로 탐욕 매칭하여 `rest=ev4`가 되면서 `rev4` 추출 실패. 결과:
    - `pr6rev4 → pr6r_en` (기대 `pr6_rev4_en`)
    - `pr7rev3, pr19rev1, pr20rev3, pr21rev2, pr28rev2, pr35rev1, PR38Rev5, pr16corr1` 모두 동일 패턴
    - `PR-1B-...-with-Annex-1.md`: `-with-annex-` 가 뒤에 붙어있음에도 false-positive로 `pr1b_annex_*` 라벨
    - `pr_02_pdf89` 등: 선행 0 정규화 안됨 (`pr02_...`)
  - **감지 시점**: Phase C 발행 직후 merge_index.json 검증에서 `iacs_pr_pr6r_en` 등 비정상 item_id 다수 확인.
  - **조치**:
    1. 초회 발행된 PR WU 5개 (`wu-iacs_pr_*__pre__content.md`) 삭제.
    2. `merge_index.json.corpora`에서 `iacs_pr`, `iacs_pr_del` scope 제거 (atomic rename).
    3. `parse_pr_filename` 정규식을 `^pr[-_\.]?(\d+)([abcd])?(?=[-_\.]|rev|corr|add|annex|del|withdrawn|$)(.*)` 로 교체 — letter suffix는 `{a,b,c,d}` 로 제한하고 뒤에 delimiter 또는 알려진 marker(rev/corr/add/annex/del/withdrawn)만 허용.
    4. `is_annex`/`is_add`/`is_del_filename`을 `re.search` → `re.match`로 변경해 PR 번호 직후 첫 토큰에서만 매칭.
    5. 선행 0 제거: `num_digits = str(int(num_digits_raw))`.
    6. Phase A → B → C 재실행.
  - **검증**: 재실행 후 item_id 전수 재확인 — `pr6_rev4_en`, `pr38_rev5_en`, `pr16_corr1_en`, `pr1b_rev8_en` 모두 정상. 48/48 스캔, 0 unparseable, 0 duplicates.
  - **심각도**: 상 (item_id 오류 방치 시 다운스트림 매핑 파괴). → 해소.

### 수행 결과

| 단계 | 결과 |
|:---|:---|
| Phase A | 48 MD 스캔, 115,638 tokens. 가산성 오류 0건, 경고 2건(±1 BPE 경계). 선행 0·letter suffix 정규화 완료. Judgments 20건 (pdf-extract revision inference + contact 다중 PR). |
| Stage 3 | `iacs_pr` 신규 Source Family 승인, [heading_profiles.json](.claude/skills/md2wu/heading_profiles.json) 등록. |
| Phase B | 단일 배치 B001 (48 items, 115,638 tokens, 600K 예산 대비 19%). claimed. reserved_for_other_session 0. |
| Phase C | WU 5개 publish (pr: 4, pr_del: 1). split=0, standalone=1(`pr38_rev5_en` 40,859 tokens, 32K 초과 1.5× 예외), merged=4. 락 전부 해제. |
| Issue gate | undersized 2건 LOW (iacs_pr_merge_df5b59ab 11,631 / iacs_pr_merge_bd292ff8 781). 자동 `processed`. |

### 발행 산출물 (전역)

```
wu-iacs_pr_merge_3cfa06b1__pre__content.md  (12 items: PR1/1B/1C/1D/1A-contact/1-Annex/2A/2B/3/5/6/7/8)
wu-iacs_pr_merge_7b1e5a34__pre__content.md  (20 items: PR9~PR35 주력)
wu-iacs_pr_merge_df5b59ab__pre__content.md  (6 items: PR36/36-contact/39/40/41/42) — 11,631 tokens LOW
wu-iacs_pr_merge_bd292ff8__pre__content.md  (9 items: deleted/withdrawn 전량 in pr_del) — 781 tokens LOW
wu-iacs_pr_pr38_rev5_en__pre__content.md    (standalone, 40,859 tokens)
merge_index.json → corpora[iacs_pr]=39 items, corpora[iacs_pr_del]=9 items
```

### 신규 아티팩트

- [phase_a_scan_pr.py](.claude/skills/md2wu/phase_a_scan_pr.py) — PR 전용 스캐너 (접두사·변형 인식, body preamble revision 스캔)
- [phase_c_execute_pr.py](.claude/skills/md2wu/phase_c_execute_pr.py) — UI 템플릿에서 PR 파라미터(DOC_TYPE, GRAMMAR_VERSION, scope 매핑) 교체
- [heading_profiles.json](.claude/skills/md2wu/heading_profiles.json)에 `iacs_pr` 엔트리 추가 (문서 경계, 파일명 변형, 알려진 이슈 포함)

### 후속 필요 작업

- `iacs_pr` 프로파일 `stats` 항목에 본 실행 결과(5 WUs / 48 items / 115,638 tokens) 반영 가능.
- 필요 시 Contact·Annex 문서를 `pr` 시리즈에서 분리해 별도 `pr_contact`·`pr_annex` series로 재편 검토 (현재는 단순성 우선으로 `pr`에 포함).
