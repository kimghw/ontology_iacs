## 2026-04-17T04:05Z — md2wu /home/kimghw/ontology_iacs/UR (session 210d725e)

### 모호/해석 보고

- **모호점**: 사용자는 `/md2wu /home/kimghw/ontology_iacs/UR`만 지정. `batch_capacity`, `source_family` 확정 여부는 명시 없음.
  - **해석**: AskUserQuestion으로 두 가지를 동시에 확인 — (1) Stage 3 `iacs_ur` 승인 여부, (2) `batch_capacity` 값.
  - **사용자 선택**: `iacs_ur` 승인 + `batch_capacity = 600K`.
  - **심각도**: 하 (대화로 즉시 해소).

- **SKILL.md vs phase_c_execute.py 불일치 (사용자 지적 → 수정 완료)**
  - **내용**: SKILL.md §"전역 발행 정책"은 `skill_md2wu/` 루트에 `wu-*__pre__content.md` + `merge_index.json` **두 종류만** 남기도록 규정. 최초 1차 실행 스크립트는 scope별 `corpus-*__pre__manifest.json`, `corpus-*__pre__item_index.json`, `corpus-*__md2wu__issue_gate_report.json`을 전역에 그대로 publish하고 `merge_index.json`은 생성하지 않음.
  - **사용자 지적**: "스킬에 머지랑 스필릿 정보만 남기라고 했는데" — 정책 위반 확인.
  - **조치**:
    1. 기존 workroot 산출물 정리 — T2 manifest·T5 issue_gate 2쌍은 세션 `out/`으로 이동, `corpus-*__pre__item_index.json` 2개는 삭제, 두 scope의 item_to_wu를 합쳐 `merge_index.json` 생성(atomic rename).
    2. [phase_c_execute.py](.claude/skills/md2wu/phase_c_execute.py) 수정 — manifest/issue_gate를 세션 `out/`에만 쓰고, 최종 단계에서 `merge_index.json`을 기존 `corpora`와 병합하여 atomic rename으로 갱신. `corpus-*__pre__item_index.json` 생성 블록 제거.
    3. [phase_b_plan.py](.claude/skills/md2wu/phase_b_plan.py) 수정 — B-0 스킵 판정이 `merge_index.json.corpora[scope].item_to_wu`를 SSOT로 사용(기존 `corpus-*__pre__item_index.json` 글롭 제거).
  - **검증**: 스크립트 수정 후 phase_b_plan 재실행 결과 — 71개(ur_s+ur_z) 정확히 스킵, 남은 209개만 새 배치에 포함됨. 해당 락은 이번 세션 처리 범위 아니므로 즉시 해제.
  - **저장**: `feedback_md2wu_publishing.md` 메모리에 정책 등록.
  - **심각도**: 중 → 해소.

### 수행 결과

| 단계 | 결과 |
|:---|:---|
| Phase A | 280 MD 스캔, 946,777 tokens, 가산성 오류 0건, 경고 8건(±1 BPE 경계) |
| Stage 3 | `iacs_ur` 승인, 기존 classification 매칭 |
| Phase B | 배치 2개. B001(ur_s, ur_z, 71 items, 499,809 tokens) claimed, B002(14개 시리즈, 209 items, 446,968 tokens) reserved_for_other_session |
| Phase C | WUs 20개 publish (ur_z: 12, ur_s: 8). split=1, standalone=11, merged=7. 락 전부 해제. |
| Issue gate | undersized 2건 (iacs_ur_merge_e8a9a124 8,529 tokens / iacs_ur_merge_42418193 13,335 tokens, 심각도 LOW). 자동 `processed`. |

### 후속 필요 작업

- 남은 배치 B002 (ur_a/c/d/e/f/g/h/i/k/l/m/n/p/w, 209 items, 446K tokens)는 **별도 세션**에서 재실행 필요 — 이번 세션은 락을 걸지 않았으므로 다음 세션이 스캔 후 바로 claim 가능.
- SKILL.md "전역 발행 정책"과 스크립트 출력 정렬이 필요하면 별도 지시 요망.
